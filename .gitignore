# References (local study material, not part of the product)
references/

# OS
.DS_Store

# Dependencies
node_modules/

# Env
.env
.env.*
!.env.example

# Foundry deps (restore with: forge install)
smartcontracts/lib/
smartcontracts/cache/
smartcontracts/out/
.gstack/

# Written by `giwacard init` — carries the local vault owner address,
# and can be made to carry the keystore passphrase. Never commit it.
.mcp.json
