/**
 * Reusable UI primitives shared with the Infinite landing page.
 *
 * These mirror the shapes already used in landingpage/src/App.tsx — rounded-full
 * controls, backdrop-blurred pills, the #18161B ink on #F4F0ED canvas — so the
 * dashboard inherits the marketing site's visual language instead of growing a
 * second design system (KTD-18).
 */
export { Button } from "./button";
export type { ButtonProps } from "./button";
export { Badge } from "./badge";
export type { BadgeProps, BadgeTone } from "./badge";
export { Panel } from "./panel";
export type { PanelProps } from "./panel";
export { Stagger } from "./stagger";
export type { StaggerProps } from "./stagger";
export { cx } from "./cx";
